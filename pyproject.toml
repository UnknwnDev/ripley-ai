[project]
name = "ripley-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "UnknwnDev", email = "0unknwn.dev@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ripley-ai = "ripley_ai:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
